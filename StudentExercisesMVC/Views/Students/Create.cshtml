@model StudentExercisesMVC.Models.ViewModels.StudentCreateViewModel

@{
    ViewData["Title"] = "Create Student";
}

<h2>@ViewData["Title"]</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="student.StuFirstName" class="control-label"></label>
                <input asp-for="student.StuFirstName" class="form-control" />
                <span asp-validation-for="student.StuFirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="student.StuLastName" class="control-label"></label>
                <input asp-for="student.StuLastName" class="form-control" />
                <span asp-validation-for="student.StuLastName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="student.StuSlackHandle" class="control-label"></label>
                <input asp-for="student.StuSlackHandle" class="form-control" />
                <span asp-validation-for="student.StuSlackHandle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Cohorts" class="control-label"></label>
                <select asp-for="student.CohortId" asp-items="@Model.Cohorts"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}